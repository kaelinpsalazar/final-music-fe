import React from 'react';
import { useNavigate, useLocation } from 'react-router-dom';

const Header = () => {

  return (
    <main>
      Welcome to SALAZAR FEST
    </main>

  );
};

export default Header;